package modifire07_object_array;

public class PersonTest {
public static void main(String[] args) {
	PersonManager mgr = new PersonManager();
	//사람 추가 / 추가 ... 많이 함
	
	PersonManager mgr2 = new PersonManager();
	//목록 가져와! -> mgr2에는 사람 안 들어 있음.
	//회사 전체가 관리하는 사람 목록은 mgr1이든 2든 동일하게 갖고 있어야 함
	// -> 클래스 변수로 관리하면 되나? -> 그걸 넘어서 
	
}
}
